# Production Environment Configuration
# คัดลอกไฟล์นี้เป็น .env.production และแก้ไขค่าตามจริง

# Node Environment
NODE_ENV=production
PORT=3000

# Database Configuration
# แก้ไขเป็นข้อมูล MySQL บน VM ของคุณ
DATABASE_URL="mysql://thai_mooc_user:YOUR_SECURE_PASSWORD@localhost:3306/thai_mooc"

# NextAuth Configuration
# สร้าง secret ใหม่ด้วยคำสั่ง: openssl rand -base64 32
NEXTAUTH_URL=https://yourdomain.com
NEXTAUTH_SECRET=YOUR_RANDOM_SECRET_HERE

# Upload Configuration
UPLOAD_DIR=/var/www/thai-mooc-clean/public/uploads
MAX_FILE_SIZE=10485760

# Optional: Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Optional: Email Configuration (ถ้าต้องการส่งอีเมล)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
