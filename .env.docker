# Docker Environment Variables
# Copy this file to .env before running docker-compose

# MySQL Configuration
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=thai_mooc
MYSQL_USER=thaimooc
MYSQL_PASSWORD=thaimoocpassword
MYSQL_PORT=3307

# Application Configuration
APP_PORT=3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Security (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-in-production-please-use-a-long-random-string

# Database URL for Prisma (automatically constructed in docker-compose)
# DATABASE_URL=mysql://thaimooc:thaimoocpassword@mysql:3306/thai_mooc
